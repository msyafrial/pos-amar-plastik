# Amar Plastik — Design System

Dokumentasi token desain untuk POS kasir **Amar Plastik** (toko bahan pembuatan kue & perlengkapan plastik, tagline "Bring the quality"). Semua nilai diambil dari CSS custom properties di `styles.css` (`:root`).

## Warna

| Token | Nilai | Kegunaan |
|---|---|---|
| `--bg` | `#f5f6fa` | Latar halaman |
| `--surface` | `#ffffff` | Kartu, panel, rail |
| `--ink` | `#1e2432` | Teks utama |
| `--muted` | `#8a94a6` | Teks sekunder / label |
| `--line` | `#eef0f5` | Border & divider |
| `--accent` | `#c8102e` | Aksen utama (merah brand Amar Plastik): kategori aktif, harga diskon, CTA, FAB |
| `--accent-soft` | `#fdeeee` | Latar lembut aksen (promo, pay aktif, rail aktif) |
| `--blue` | `#3d7bfd` | Badge diskon |
| `--blue-soft` | `#edf2ff` | Latar lembut biru |
| `--green` | `#1faa59` | Status stok aman / badge Ready |
| `--yellow` (hardcoded) | `#f2a30f` | Badge Stok Menipis (stok ≤ 5) |
| `--red` (hardcoded) | `#e0342b` | Badge Stok Habis (stok = 0) |
| `--gray` (hardcoded) | `#8a94a6` | Badge EXP (kedaluwarsa) |
| Merah error | `#e0342b` | Stok rendah, hover hapus/keluar (hardcoded) |

### Sistem badge produk
Setiap kartu produk bisa menampilkan beberapa tag sekaligus (misal Diskon + Ready + EXP sekaligus — 3 tag):
- **Diskon N%** — biru `#3d7bfd`, muncul jika produk punya harga `old`
- **Ready** — hijau `#1faa59`, stok aman (> 5)
- **Stok Menipis** — kuning `#f2a30f`, stok ≤ 5 (menggantikan Ready)
- **Stok Habis** — merah `#e0342b`, stok = 0 (menggantikan Ready/Stok Menipis)
- **EXP MM/YY** — abu `#8a94a6`, hanya produk dengan tanggal kedaluwarsa

Wadah `.card-badges` memakai `display: flex; flex-wrap: wrap; gap: 4px` sehingga 3 tag yang tidak muat satu baris otomatis pindah ke baris kedua tanpa overflow.

### Kartu stok habis (disabled)
Saat `stock = 0`:
- `<button class="card">` diberi atribut `disabled` + `aria-disabled="true"` — tak bisa diklik keyboard maupun mouse
- Media kartu memakai **gradasi abu** `linear-gradient(135deg, #eceef2, #d9dde4)` (menggantikan tint warna)
- Emoji produk **grayscale** (`filter: grayscale(1); opacity: .5`)
- Nama, harga, dan meta diredam (`opacity: .55–.75`)
- Efek hover angkat dimatikan; cursor `not-allowed`
- Guard di `addToCart()`: klik paksa tetap ditolak + toast peringatan

### Gradasi & bayangan
- Avatar: `linear-gradient(135deg, #f7d4d4, #e5a1a1)`
- `--shadow-sm`: `0 1px 2px rgba(30,36,50,.05)` — kartu & input
- `--shadow-md`: `0 10px 30px -12px rgba(30,36,50,.18)` — hover kartu, panel, toast
- Bayangan aksen: `0 10px 20px -8px rgba(200,16,46,.55)` untuk elemen merah terangkat

## Tipografi

- **Font**: `Plus Jakarta Sans` (Google Fonts), fallback `system-ui`
- **Skala**:
  - Total/CTA: 20px / 800
  - Judul menu: 19px / 800
  - Nama item, label panel: 13–14px / 700–800
  - Body sekunder: 11–12.5px / 500–600
  - Badge/meta: 9.5–10.5px / 600–800
- **Angka**: semua harga memakai `font-variant-numeric: tabular-nums`
- **Format mata uang**: `Rp 21.200` (pemisah ribuan titik, `id-ID`)

## Struktur File Multi-Halaman (HTML Terpisah)

Aplikasi dipecah menjadi file-file HTML mandiri, saling terhubung via tautan sidebar (`.rail` dengan `<a>`):
- `index.html` — **Dashboard Utama** (halaman beranda / landing default, analitik penjualan, hero cards gradien, filter rentang waktu kalender, chart kurva tren, top produk & pelanggan)
- `kasir.html` — **Kasir** (halaman POS lengkap dengan menu, panel order, promo, popup modal tunai, dan popup struk)
- `transaksi.html` — **Riwayat Transaksi** (kartu-kartu riwayat dari `localStorage`, tombol kosongkan riwayat)
- `produk.html` — **Master Data Produk** (4 kartu statistik, toolbar cari/filter/ekspor/tambah, tabel 8 kolom dengan SKU/batch/kedaluwarsa/status pill, aksi edit/hapus via modal konfirmasi, pagination 6 item)
- `kategori.html` — **Master Kategori** (katalog kategori produk toko, kartu visual dengan artwork cover, switcher Grid/List view, pencarian live, modal tambah/edit dan hapus)
- `laporan.html` — **Laporan Penjualan** (analisis dan ringkasan omzet, 4 kartu KPI hero penjualan, panel filter terintegrasi, dan tabel penjualan 8 kolom)
- `profit.html` — **Laporan Keuntungan / Profit** (analisis laba kotor & margin, 4 kartu KPI hero profit, ringkasan kalkulasi profit tabel penjualan)
- `pengaturan.html` — **Pengaturan** (informasi toko dari object `STORE`, pajak, versi aplikasi)

### Shared Assets (Organized in `assets/`)
- `assets/css/shared.css` — gaya layout tunggal `.app.app-single` (tanpa panel order, grid 72px 1fr), `.page-pad.page-full` / `.page-full` (lebar 100% mengisi area kanan penuh untuk data table), kartu, tabel, modal, status pill, toast
- `assets/js/shared.js` — data `CATEGORIES`, `MENU` (dengan SKU & batch), `STORE`, helper `loadHistory()` / `saveHistory()` via `localStorage` (agar transaksi di Kasir otomatis terbaca di Transaksi & Laporan), format `rp()`, toast, modal helper
- `assets/css/styles.css` — token desain dasar, sidebar berkategori, topbar, grid kartu produk, panel order kasir, popup tunai, dan struk
- `assets/js/app.js` — logika khusus kasir (`kasir.html`): keranjang, hitung total/diskon/pajak, konfirmasi tunai, cetak struk
- `assets/images/logo.jpg` — logo toko resmi Amar Plastik untuk header sidebar navigasi

## Layout shell

- Radius: `--radius: 16px` (kartu), 12–14px (input, chip, tombol), 999px (pill/FAB)
- Grid menu: `repeat(auto-fill, minmax(168px, 1fr))`, gap 16px
- Layout: `grid-template-columns: 72px | 1fr | 340px` (rail · konten · order)
- Breakpoint utama: **1080px** — panel order jadi drawer + FAB muncul; **640px** — grid lebih rapat, teks profil disembunyikan

## Komponen

| Komponen | Kelas | Catatan |
|---|---|---|
| Rail navigasi | `.rail`, `.rail-btn`, `.rail-section-label`, `.rail-section-divider` | **Modern Floating Capsule Sidebar dengan Pengelompokan Kategori**: Brand logo header Amar Plastik di atas (`.rail-brand`), navigasi terstruktur rapi ke dalam 4 kategori hierarkis (**UTAMA**: Dashboard, Kasir, Transaksi; **KATALOG**: Produk, Kategori; **LAPORAN**: Penjualan, Keuntungan; **SISTEM**: Pengaturan). Dilengkapi `.rail-section-label` (10px, bold 800, uppercase, letter-spacing 0.08em) saat expanded dan garis divider halus `.rail-section-divider` saat collapsed. Ikon SVG didesain proporsional dan relevan untuk setiap modul. Popover tooltip melayang saat hover di mode collapsed, profile footer di bawah (`.rail-profile`). Mode ringkas 72px ⇄ mode terbuka 240px dengan transisi spring `220ms cubic-bezier(0.16, 1, 0.3, 1)`. Otomatis menutup saat klik di luar area sidebar |
| **Floating Edge Toggle** | `.rail-edge-toggle` | Tombol bulat putih mengambang persis di perbatasan kanan sidebar (`right: -12px`, `top: 24px`, sejajar vertikal dengan logo brand). Menampilkan ikon `›` saat collapsed (klik untuk buka) dan `‹` saat expanded (klik untuk tutup). Didukung shortcut keyboard `Ctrl+B` / `Cmd+B` dan persistensi di `localStorage` |
| **Brand Logo Header** | `.rail-brand`, `.rail-logo`, `.rail-logo-img` | Logo resmi Amar Plastik (`assets/images/logo.jpg`) dengan warna asli (tas dan teks merah maroon di atas latar putih bersih, tanpa di-reverse), menggunakan `object-fit: contain` agar rasio aspek tetap proporsional dan tidak terdistorsi/ketarik (`42x42px`, radius 12px, border halus) beserta teks "AMAR PLASTIK" dan tagline "Bring the quality" saat sidebar terbuka. Tampil di bagian paling atas sidebar |
| **Profile Footer** | `.rail-profile` | Avatar profil kasir (LM) beserta label KASIR TOKO dan nama Lina Puspa Melinda, diposisikan di bagian paling bawah sidebar dengan divider halus |
| **Search bar produk** | `.search` | Diposisikan di samping tombol filter pada bagian header katalog produk (`.menu-head-tools`); tinggi 40px, border `--line`, border-radius 11px, focus glow aksen merah, placeholder "Cari produk…" |
| **Filter & Sorting Popover** | `.filter-dropdown-wrap`, `.filter-btn`, `.filter-popover` | Tombol filter interaktif di samping input search yang memunculkan popover dropdown menu (`.filter-popover`): filter status stok (**Semua, Ready, Diskon, Stok Menipis**) & sorting (**Rekomendasi, Harga Terendah/Tertinggi, Nama A-Z, Stok Terbanyak**). Dilengkapi tombol **Terapkan**, **Reset**, badge aktif (`.filter-badge`), dan auto-close saat klik di luar |
| Kartu menu | `.card`, `.card-media`, `.card-media-img` | Menampilkan foto asli produk dari direktori `images/product/` dengan `object-fit: cover` dan hover zoom halus 1.06x (`.card-media-img`). Jika belum ada foto, fallback ke emoji berlatar tint pastel. Dilengkapi badge multi-tag mengambang di pojok kiri atas |
| Badge | `.badge` di dalam `.card-badges` | Multi-tag, wrap otomatis: `is-blue` diskon, `is-green` ready, `is-yellow` stok menipis, `is-gray` EXP |
| Item pesanan | `.order-item`, `.oi-thumb`, `.oi-thumb-img` | Grid `grid-template-areas: "thumb info price" / "thumb qty price"`; gap 12px konsisten, nama ellipsis, harga rata kanan. Thumbnail pesanan (`.oi-thumb.has-img`) menampilkan foto asli produk dari `images/product/` berdimensi 44x44px rounded 11px dengan border halus. Sublabel menampilkan kategori + stok; tombol + otomatis `disabled` saat qty = stok |
| Total | `.totals`, `.grand-total` | Subtotal (harga normal), Diskon Produk (aksen), Pajak 10%, lalu divider sebelum Total |
| Metode bayar | `.pay-btn` | Radio group, aktif = merah. Opsi: Cash, Debit, QRIS |
| CTA | `.cta` | "Proses Transaksi", **hijau** `--green` penuh + bayangan hijau, disabled saat keranjang kosong; klik memunculkan konfirmasi dulu |
| FAB keranjang | `.cart-fab` | Hanya tampil pada layar ≤768px (mobile & tablet portrait) saat drawer pesanan tertutup (`.is-hidden` / `body:has(.order.is-open)` otomatis `display: none !important`), dengan badge hitung jumlah item |
| **Drawer Pesanan** | `.order`, `.order-backdrop`, `.order-close-btn` | Pada layar ≤768px, panel pesanan beralih menjadi drawer off-canvas di sisi kanan (`width: min(380px, 92vw)`, `z-index: 96`) dengan tombol tutup `✕` dan backdrop blur (`.order-backdrop`). Saat terbuka, FAB keranjang otomatis disembunyikan sepenuhnya agar tidak menumpuk. Dilengkapi **Integrasi Mobile Back Navigation (2026 Mobile UX Standard)**: saat drawer terbuka di layar mobile/smartphone, menekan tombol navigasi Back (tombol back hardware/gesture Android maupun browser back button) atau tombol Escape keyboard akan **langsung menutup drawer ringkasan pesanan** secara mulus tanpa keluar dari halaman kasir. Setelah transaksi selesai diproses atau struk ditutup, drawer pesanan otomatis tertutup kembali (`toggleOrderPanel(false)`). Di layar tablet landscape (1024px) & desktop (>768px), panel pesanan tetap docked di kolom kanan dengan proporsi rapi (320px) |
| **Mobile Sidebar Toggle** | `.topbar-mobile-toggle` | Tombol toggle navigasi sidebar di topbar yang hanya aktif pada viewport ≤768px (`display: none` di desktop/tablet landscape), menggunakan ikon **garis tiga (hamburger) modern & clean** berdimensi 40x40px, rounded 11px, stroke halus 2.2px dengan round caps, border tipis, dan subtle elevation untuk kemudahan akses membuka drawer sidebar navigasi |
| **Sticky Topbar Mobile** | `.topbar` | Pada layar ≤768px, topbar dipertahankan tetap mengambang di atas (`position: sticky; top: 0; z-index: 40`) dengan efek translusen dan latar blur (`backdrop-filter: blur(10px)`) serta pembatas bawah halus, sehingga kasir tetap memiliki akses navigasi cepat saat melakukan scroll katalog produk |
| Modal konfirmasi | `.modal-backdrop` + `.modal` | Popup kustom menggantikan `confirm()`: ikon, judul, isi, tombol Batal / Ya (varian `danger` merah atau `success` hijau); tutup via tombol, klik backdrop, atau Escape |
| Box pembayaran tunai | `.cash-box` | Hanya muncul di modal saat metode **Cash**: label "Uang Dibayarkan", input manual (format ribuan otomatis, **default terisi total belanja**), tombol cepat **50.000 / 100.000** + dropdown **Lainnya…** (150rb–500rb), mini tabel kalkulasi Total / Dibayar / Kembalian (hijau; merah "− Rp N" bila uang kurang, tombol OK terblok). Semua pilihan di-reset tiap kali modal dibuka |
| Popup struk | `.receipt-backdrop` + `.receipt` | **Hitam-putih**, font Courier (monospace): logo tas SVG, AMAR PLASTIK + tagline, alamat & telp, No. Transaksi (TRX-YYYYMMDD-XXXX), waktu, kasir, pembayaran, rincian item — item berdiskon menampilkan baris tambahan `Harga normal Rp N (dicoret) − Rp N` (`.ri-disc`) — subtotal/diskon/pajak/TOTAL, **Dibayar & Kembalian**, ucapan terima kasih. Isi struk (`.receipt-body`) scroll mandiri; footer tombol (`.receipt-actions`) **selalu terlihat** — **🖨 Cetak** (outline, stub printer via toast) & **Selesai** (hitam solid, menutup struk + toast) |
| Toast | `.toast` | Bawah tengah, auto-hide 2.6s |
| **Dashboard Utama (Modern Bento POS Analytics 2025–2026)** | `index.html`, `.db-header`, `.db-store-status-pill`, `.db-time-filter`, `.db-cta-kasir-btn`, `.db-kpi-grid`, `.db-kpi-card`, `.db-analytics-grid`, `.db-chart-panel`, `.db-payment-panel`, `.db-pay-multi-bar`, `.db-operations-grid`, `.db-stock-alert-section` | **Dashboard Analitik Retail & Kasir Modern Bertema Putih Bersih (Clean White SaaS Bento Grid)**: Mengadopsi arsitektur Bento Grid kontemporer yang menggantikan kartu herois yang terlalu memakan ruang dan metrik statis yang tidak bernilai operasional. Menyajikan metrik finansial, kesehatan stok, dan distribusi kas secara komprehensif tanpa data duplikasi. |
| **Bento KPI Cards (4 Indikator Finansial & Operasional)** | `.db-kpi-grid`, `.db-kpi-card`, `.db-kpi-head`, `.db-kpi-tag`, `.db-kpi-value`, `.db-kpi-sub` | Empat kartu bento berlatar putih bersih dengan soft shadow, micro-hover elevation (-2px), dan fluid typography `clamp(20px, 1.8vw, 27px)` dengan dukungan multi-digit (ratusan juta/miliaran tanpa elipsis): (1) **Total Omzet Penjualan** (kartu primer dengan top accent bar indigo dan tag real-time), (2) **Laba Bersih** (warna emerald green dengan badge margin ~20%), (3) **Total Transaksi & Nilai Rata-rata** (volume nota dalam bahasa Indonesia ramah orang awam: subteks 'Rata-rata: Rp N' menggantikan akronim 'AOV', nilai 'N Transaksi' menggantikan 'Order', dan tag pill '● Nota Selesai' menggantikan 'Volume Order'), serta (4) **Kesehatan Inventaris** (jumlah SKU aktif dan counter alert stok menipis/habis) |
| **Grafik Tren Penjualan Area SVG** | `.db-chart-container`, `.db-chart-wrap`, `.db-chart-y-labels`, `.db-chart-svg`, `.db-chart-footer` | Visualisasi tren penjualan dengan kurva halus cubic bezier SVG dan gradien ungu lembut (`#4f46e5`). Dilengkapi sumbu Y vertikal tabular-nums dengan **Smart Compact Formatter** (`formatCompactRp` untuk angka jutaan/ratusan juta/miliar seperti `Rp 700 Jt`, `Rp 1,5 M`), sumbu X, serta statistik ringkas footer: Puncak Penjualan dan Rata-rata Harian |
| **Distribusi Pembayaran & Kas Laci** | `.db-payment-panel`, `.db-pay-multi-bar`, `.db-pay-seg`, `.db-pay-row`, `.db-cash-drawer-box` | Rekapitulasi metode pembayaran toko: segmented multi-color progress bar (Tunai `#10b981`, QRIS `#7c3aed`, Debit `#0284c7`), daftar breakdown nominal & persentase per metode, serta kartu rekapitulasi uang fisik kasir masuk (**Uang Fisik Kasir Masuk / Cash Drawer**) untuk rekonsiliasi kasir harian |
| **Feeds Operasional (Top Produk, Profit Tertinggi & Transaksi Live)** | `.db-operations-grid`, `.db-top-list`, `.db-top-item`, `.db-top-rank.is-profit`, `.db-top-bar-fill.is-profit`, `.db-top-profit-val`, `.db-top-share-tag.is-profit`, `.db-recent-list`, `.db-recent-item` | Tiga panel feed operasional dalam grid responsif (3 kolom desktop, 2+1 kolom tablet/laptop, 1 kolom mobile): (1) **Produk Terlaris** (5 produk teratas dengan ranking bulat, thumbnail emoji, bar persentase omzet, kuantitas terjual, dan total omzet per produk), (2) **Produk Profit Tertinggi (Laba Bersih)** (5 produk kontribusi laba terbesar dengan badge rank hijau emerald, emoji produk, bar gradien emerald `#10b981`, nominal profit bertanda `+Rp N`, margin %, dan kontribusi persentase laba bersih terhadap total laba toko), serta (3) **Aktivitas Transaksi Terbaru** (5 transaksi live terakhir dengan kode TRX, badge metode bayar, timestamp, nama pelanggan, kasir yang bertugas, dan nominal transaksi) |
| **Peringatan Inventaris & Restock Produk** | `.db-stock-alert-section`, `.db-stock-alert-card`, `.db-stock-badge-item`, `.db-stock-pill` | Banner peringatan inventaris dinamis di bagian bawah dashboard yang mendeteksi produk dengan stok menipis (`stok <= 5`) atau habis (`stok = 0`) dengan badge pill oranye/merah dan tautan langsung "+ Kelola di Master Produk →". Banner otomatis beralih menjadi status hijau aman jika seluruh persediaan terpenuhi |
| **Header & Filter Rentang Waktu** | `.db-header`, `.db-store-status-pill`, `.db-time-filter`, `.db-chip`, `.db-cta-kasir-btn` | Header dashboard dengan status live toko `● Toko Buka • Kasir Aktif`, tombol aksi cepat `+ Kasir Baru` di pojok kanan atas, serta kontrol filter rentang waktu instan (**Hari Ini, Minggu Ini, Bulan Ini, Custom**) bergaya iOS Segmented Pill Bar 1-baris yang ramping (hanya 34px) sehingga menghemat ruang vertikal secara optimal di tampilan mobile |
| **Mobile First-View Optimization & Alignment Simetris** | `.db-header`, `.db-kpi-grid`, `.db-kpi-card.is-primary`, `.db-kpi-card.is-stat-card`, `.db-kpi-card.is-inventory` | Tata letak adaptif khusus mobile (`≤768px`) yang menempatkan: (1) Header navigasi ringkas dengan tombol `+ Kasir Baru` sejajar judul, (2) 1-row segmented filter pill, (3) Kartu Master Finansial Hero full width, (4) **Kartu Sekunder Laba Bersih & Total Transaksi Berdampingan Sejajar Presisi (Pixel-Perfect Alignment)**: lock min-height `.db-kpi-head` 42px, padding 12px 11px, icon 26px, `white-space: nowrap` pada label & tag bullet `●`, penyetaraan baseline Y teks nominal dan subteks 1 baris bebas overflow, (5) Strip status inventaris horizontal, serta (6) Grafik Tren Penjualan SVG yang langsung tampak above-the-fold |
| **Halaman Master Kategori** | `kategori.html`, `.cat-header`, `.cat-toolbar`, `.cat-grid`, `.cat-card` | Halaman katalog kategori toko sesuai referensi desain: header counter "8 kategori terdaftar", tombol pill ungu "+ Tambah Kategori", toolbar pencarian live search ("Cari kategori...") dan toggle tampilan Grid/List. Kartu kategori modern dengan artwork ilustrasi sampul SVG, judul tebal, subtitle deskripsi, serta action pill buttons (Edit & Hapus) yang muncul melayang di pojok kanan atas kartu saat hover. Didukung modal dialog tambah/edit dan persistensi `localStorage` (`ap_master_categories`) |
| **Kartu Kategori & Aksi** | `.cat-card`, `.cat-card-media`, `.cat-card-actions`, `.cat-action-btn` | Kartu kategori rounded 18px berlatar putih dengan border halus `#edf2f7`, rasio aspek media 16:10, hover elevation `-3px`. Tombol aksi edit & hapus mengambang dengan latar semi-transparan `rgba(255,255,255,0.92)` dan backdrop-filter blur. Mendukung mode List View (`.is-list-view`) dengan kartu horizontal |
| **Modal Kategori** | `.cat-modal`, `.cat-form`, `.cat-input` | Modal dialog rounded 20px untuk menambah atau menyunting nama kategori, deskripsi, dan template sampul dengan validasi form dan feedback toast interaktif |
| **Produk Dual View (Card vs List)** | `produk.html`, `.prod-view-toggles`, `.prod-view-btn`, `.prod-cards-grid`, `.prod-card-item` | Mode tampilan ganda pada halaman Master Produk: **Card View** (kartu visual produk dengan thumbnail emoji pastel, badge stok gelap `Stok: N`, badge kategori ungu, nama produk, kode SKU, perbandingan Harga Beli dan Harga Jual biru tebal, serta tombol aksi Edit/Hapus saat hover) dan **List View** (tabel komprehensif 8 kolom dengan SKU, batch, kedaluwarsa, stok, harga jual, dan status pill). Pilihan mode disimpan persisten di `localStorage` (`ap_prod_view_mode`) |
| **Riwayat Transaksi (Log Transaksi Kasir & Audit Trail)** | `transaksi.html`, `.trx-page-header`, `.trx-table-card`, `.trx-table`, `.trx-btn-filter`, `.trx-btn-new`, `.trx-badge-customer`, `.trx-badge-pay`, `.trx-badge-status`, `.trx-badge-item`, `.trx-col-profit` | Halaman riwayat transaksi kasir operasional dengan kartu tabel putih bersih ber-shadow halus. Header menampilkan judul "Riwayat Transaksi" dengan ikon jam/history ungu indigo, subtitle dinamis jumlah transaksi, tombol outline "Filter" dengan popover rentang waktu & live search, serta tombol solid indigo pill "Transaksi Baru" ke Kasir. Kolom tabel 11 kolom: **NO, INVOICE (tebal), TANGGAL, KASIR, PELANGGAN (soft grey pill badge), METODE BAYAR (badge Tunai 💵, QRIS 📱, Debit 💳), ITEM (circle count badge), STATUS (badge hijau ● Selesai), TOTAL (tebal), PROFIT (hijau emerald +Rp N tabular), dan STRUK (icon printer)**. |
| **Laporan Penjualan (2026 Ultra-Modern Luxury Bento Analytics & Decision-First Architecture)** | `laporan.html`, `styles.css`, `.rpt-bento-grid`, `.rpt-bento-card`, `.rpt-bento-head`, `.rpt-bento-label-wrap`, `.rpt-bento-label`, `.rpt-bento-tag`, `.rpt-bento-icon`, `.rpt-bento-body`, `.rpt-bento-val-row`, `.rpt-bento-sparkline`, `.rpt-bento-value`, `.rpt-bento-sub`, `.rpt-bento-sub-text` | **Pembaruan Bento Analytics Grid 2026 (Fintech Luxury Aesthetic, Deep Obsidian Typography & Refined Metallic Palette)**: (1) **Fintech Luxury Typography (Inter Font)**: Mengadopsi font modern `Inter` dengan rendering tajam (-0.04em letter spacing, multi-weight 400-850, `font-variant-numeric: tabular-nums` dan font-feature-settings), menggantikan nuansa font standar. Warna angka utama menggunakan **Deep Obsidian Slate (`#0f172a`)** yang mewah, tegas, dan kontras tinggi; (2) **Muted & Refined Color Palette**: Menggantikan warna neon/pastel terang dengan palet warna mahal: Royal Indigo (`#4338ca`), Rich Forest/Sage Emerald (`#047857` / `#f0fdf4`), Slate Sky Blue (`#0369a1` / `#f0f9ff`), serta Burnished Bronze & Champagne Gold (`#92400e` / `#fffdf5` & `#fef3c7`); (3) **Desktop 4-Card Bento Surface**: Radius 18px berlatar frosted white dengan border semi-transparan `rgba(226, 232, 240, 0.8)`, top accent bar 3px, dan multi-layer shadow halus (`0 14px 32px -6px rgba(15, 23, 42, 0.08)`); (4) **Mobile 2026 Single Unified Master Analytics Card**: Pada layar HP mobile (360x740), 4 kartu bento menyatu rapi dalam **satu kartu master kaca terpadu (Unified Master Card)** ber-radius 20px dengan header hero omzet obsidian & sparkline, 2 kapsul sage emerald & slate sky di tengah, serta showcase bronze di bawah. Memberikan impresi visual setara app finansial premium global (Mercury, Stripe, Apple Wallet). |
| **Mobile Card & Accordion Table View (Laporan Penjualan)** | `laporan.html`, `styles.css`, `.rpt-mobile-cards`, `.rpt-mcard`, `.rpt-mcard-header`, `.rpt-mcard-primary`, `.rpt-mcard-rank`, `.rpt-mcard-name`, `.rpt-mcard-chevron`, `.rpt-mcard-metrics`, `.rpt-mmetric`, `.rpt-mcard-collapse`, `.rpt-mdetail-grid`, `.rpt-mbar-wrap` | **Transformasi Tabel Mobile ke Card + Accordion Dropdown (≤640px)**: Menggantikan scroll horizontal tabel yang melelahkan di layar kecil dengan kartu sentuh modern: (1) **Tampilan Tertutup (Default)**: Setiap produk/transaksi tampil sebagai kartu compact elegan berisi informasi kunci — Nomor Rank/Waktu, Badge Kategori/Toko, Emoji + Nama Produk, Chevron status, serta 3 metrik esensial (Omzet/Subtotal, Profit hijau, Kuantitas terjual) dalam strip container abu-abu rapi; (2) **Interaksi Klik / Dropdown Accordion**: Mengetuk/klik kartu secara instan meng-expand rincian lengkap via transisi CSS `grid-template-rows: 0fr -> 1fr` halus dengan rotasi 180° pada ikon chevron: SKU produk, Rata-rata Penjualan/Hari beserta omzet harian, bar persentase kontribusi omzet & profit (pada Tab Rekap), atau No. Invoice, **Metode Pembayaran (badge Tunai/QRIS/Debit)**, Kasir, Harga Beli modal, Harga Jual, Kategori, dan SKU Produk (pada Tab Log Item); (3) **Isolasi Desktop & Tablet**: Pada desktop (>640px) kartu mobile otomatis `display: none` dan tabel data 10/12 kolom tetap tampil normal utuh tanpa gangguan. |
| **2026 Mobile Horizontal Filter Chips** | `laporan.html`, `styles.css`, `.rpt-mobile-chip-row`, `.rpt-chip-scroll`, `.rpt-mchip`, `.rpt-mchip-sort-wrap`, `.rpt-mchip-sort-btn`, `.rpt-mchip-sort-select` | **Pola Filter Mobile 2026 (Scrollable Chips & Floating Quick Sort/Payment)**: Menggantikan dropdown klasik yang kaku dan bertumpuk vertikal dengan: (1) **Horizontal Scrollable Filter Chips**: Pill kapsul rounded 999px dengan scroll horizontal halus tanpa scrollbar (`scrollbar-width: none`), 1-tap select instan (Semua, Bahan Kue, Plastik, Kemasan), status aktif solid indigo `#4f46e5` dengan soft elevation; (2) **Quick Sort / Quick Payment Action Pill**: Tombol bulat di sisi kanan chips dengan icon filter/sort/kartu yang memicu dropdown native cepat; (3) **Two-Way Synchronization**: Perubahan pada chip mobile otomatis tersinkronisasi ke dropdown desktop/tabel dan sebaliknya, menjaga konsistensi state data analitik. |
| **2026 Streamlined Header Controls (Tri-Mode Smart Toolbar, Interactive Datepicker, Quick Stepper Navigation & 360px Adaptive)** | `laporan.html`, `styles.css`, `.rpt-header-controls`, `.rpt-top-ctrl-box`, `.rpt-top-icon-badge`, `.rpt-top-mini-label`, `.rpt-period-group`, `.rpt-period-type-pills`, `.rpt-datepicker-trigger-wrap`, `.rpt-dp-step-btn`, `.rpt-datepicker-btn`, `.rpt-calendar-popover`, `.rpt-cal-day-cell`, `.rpt-cal-grid-months`, `.rpt-cal-month-cell` | **Modernisasi Kontrol Header Toko & Periode (Harian, Mingguan, Bulanan) dengan Interactive Calendar Datepicker & Quick Direct Stepper (2026 UX)**: (1) **Filter Lokasi Toko Segmented Capsule**: Ikon toko dalam badge abu-abu soft, micro-label "LOKASI" kapital 8.5px tebal, native select borderless, dan custom chevron minimalis; (2) **Quick Direct Steppers (`.rpt-dp-step-btn`) Kiri & Kanan**: Tombol panah kiri dan kanan di samping label tanggal untuk berpindah langsung ke hari/minggu/bulan sebelumnya atau berikutnya secara cepat tanpa wajib membuka dialog kalender. Tombol "Berikutnya" otomatis ter-disable jika telah mencapai periode terkini; (3) **Unified Modern Popover Kalender dengan Multi-Year Navigation**: Ketiga mode waktu (Harian, Mingguan, Bulanan) menggunakan UI terpadu yang rapi. Mode **Bulanan** menampilkan grid 12 bulan (3 kolom x 4 baris) dan tombol panah navigasi tahun (`<` dan `>`) di header kalender sehingga pengguna dapat bebas berpindah dan meninjau laporan tahun-tahun sebelumnya (2025, 2024, dst.) maupun kembali ke tahun 2026, dengan disable bulan di masa mendatang (> Sep 2026). Mode **Harian** memungkinkan pemilihan bebas hari transaksi masa lalu dengan disable hari esok. Mode **Mingguan** mengunci hari Senin sebagai awal pekan 7 hari. (4) **Top-Anchored Stabilitas Vertikal**: Popover selalu menempel rapi dari atas (`transform-origin: top right`), dan grid hari memiliki baris tetap (6 baris x 7 kolom) sehingga bagian atas kartu kalender tidak pernah bergeser naik-turun saat berpindah bulan dengan jumlah minggu yang berbeda; (5) **360px Ultra-Compact Mobile Layout**: Penyesuaian fleksibel untuk layar HP 360x740. |
| **Tri-Tab Navigation & Audit Trail Log Transaksi (Laporan Penjualan)** | `laporan.html`, `styles.css`, `.rpt-tabs-bar`, `.rpt-tab-btn`, `#panelSummary`, `#panelItems`, `#panelTrx`, `.rpt-table-log-trx`, `#receiptBackdrop` | **Arsitektur Tiga Tampilan Analitik Penjualan Terpadu (Tri-Tab System)**: Menyediakan 3 panel terisolasi namun tersinkronisasi dengan filter toko dan periode: (1) **Tab Rekap**: Agregasi performa produk (SKU, omzet, profit, persentase kontribusi); (2) **Tab Log Item**: Rincian transaksi granular per baris produk dengan modal harga beli vs harga jual; (3) **Tab Log Transaksi**: Audit trail per faktur/nota kasir (Invoice, Waktu, Outlet Toko, Kasir bertugas, Pelanggan, Metode Bayar, Jumlah item, Total Pembayaran, Laba Bersih, dan Tombol Cetak/Lihat Struk). Dilengkapi modal struk popover interaktif (`#receiptBackdrop`), kartu akordion mobile responsif, filter cepat metode bayar, dan sinkronisasi otomatis ke 4 hero KPI stat cards di atas. |
| **Bento KPI Grid Decoupled Architecture** | `laporan.html`, `updateBentoKpis()`, `.rpt-bento-grid`, `.rpt-bento-card` | **Stabilitas & Reaktivitas Metrik Eksekutif (Decoupled Bento KPI)**: Kartu Bento KPI di bagian atas toko didesain independen dan reaktif sebagai *single source of truth* performa toko: angkanya murni merefleksikan seluruh penjualan pada Lokasi Toko dan Periode Waktu terpilih (langsung tersinkronisasi dan terbarukan otomatis saat pengguna beralih antara mode **Harian**, **Mingguan**, dan **Bulanan** maupun navigasi kalender), dan **TIDAK berubah-ubah** saat pengguna memfilter kategori, mencari nama barang, atau mengurutkan tabel data di bawahnya. Metrik tabel dan akumulasi footer tabel di bawah secara mandiri merefleksikan subset data yang difilter. |
| **2026 Enterprise Search & Scalable Category Combobox Toolbar** | `laporan.html`, `styles.css`, `.rpt-toolbar-2026`, `.rpt-search-box-2026`, `.rpt-cat-combobox-wrap`, `.rpt-cat-combobox-btn`, `.rpt-cat-popover`, `.rpt-cat-search-wrap`, `.rpt-cat-list`, `.rpt-cat-item-btn` | **Pola Filter Skalabel Menghadapi Puluhan Kategori (Enterprise 2026)**: Menggantikan segmented pills horizontal yang kaku dengan arsitektur filter scalable: (1) **Live Instant Search Box**: Input pencarian responsif berikon loupe dengan tombol clear `✕` untuk memfilter nama produk dan kode SKU secara real-time; (2) **Searchable Category Combobox (Popover)**: Tombol pemicu `[ 🏷️ Kategori: Semua ▾ ]` dengan badge hitung jumlah produk aktif. Ketika diklik, membuka popover melayang dengan **Internal Instant Search Input** (`🔍 Cari dari daftar kategori...`), daftar kategori scrollable (max-height 230px) dengan counter SKU, status centang `✓` aktif, dan interaksi tutup klik di luar (*click-outside dismiss*); (3) **Mobile Adaptive Layout**: Pada mobile (≤768px), toolbar bertransisi ke layout stacked yang kokoh tanpa ada elemen yang terpotong. |
| **Enterprise Dual-Action Sorting Controller (Field + ASC/DESC Toggle)** | `laporan.html`, `styles.css`, `.rpt-sort-group-2026`, `.rpt-sort-metric-wrap`, `#filterSortMetric`, `.rpt-sort-dir-btn`, `#btnSortDirection`, `#sortDirIcon`, `#sortDirTag`, `.th-sortable` | **Sistem Sorting Terpisah Sesuai Standar SaaS Global 2026 (Linear / Stripe / Shopify)**: Memisahkan metrik urutan dari arah urutan: (1) **Sort Metric Selector**: Dropdown minimalis untuk memilih atribut (`⚡ Total Omzet`, `💎 Total Profit`, `📦 Unit Terjual`, `📅 Rata²/Hari`, `📊 Kontribusi Profit`, `🔤 Nama Produk`); (2) **Dedicated Direction Toggle Button (`[ ↑↓ ]`)**: Tombol aksi satu-klik yang membalik arah urutan (**Ascending** $\leftrightarrow$ **Descending**) secara instan dengan feedback label tag (`DESC` / `ASC`), ikon panah SVG dinamis, dan tooltip informatif; (3) **Header Kolom Interaktif (`.th-sortable`)**: Mengklik header tabel tetap didukung dan langsung tersinkronisasi dua arah dengan controller sort di atas. |
| **Mobile Filter Bottom Sheet Modal (2026 Enterprise Slide-up)** | `laporan.html`, `styles.css`, `#btnMobileFilterModal`, `#mobileFilterModalBackdrop`, `.rpt-mfilter-sheet`, `.rpt-mfilter-sheet-body`, `#btnMobileFilterReset`, `#btnMobileFilterApply`, `.rpt-mfilter-cat-list` | **Pola Modal Filter Slide-Up Khusus Mobile (iOS / Android 2026 Standard) & Teruji Skalabilitas Puluhan Kategori**: Pada layar smartphone (≤768px), toolbar menyajikan tombol filter icon ramping `[ ⛯ ]` dengan dot status ungu jika filter aktif. Ketika disentuh/diklik, modal bottom sheet meluncur mulus dari bawah layar dengan backdrop blur semi-transparan: (1) **Kategori Produk Skalabel (Dukungan Puluhan Kategori)**: Tersedia *Instant Search Input* internal (`Cari nama kategori...`) dan kontainer list kategori scrollable vertikal (`max-height: 180px`, `scrollbar-width: thin`) dengan radio selector dan counter SKU per kategori. Teruji tetap stabil, tidak merusak layout, dan sangat cepat saat terdapat 30+ hingga ratusan kategori; (2) **Metrik Urutan**: Dropdown pilihan parameter urut bersih tanpa emoji; (3) **Arah Urutan (Segmented Pills)**: Kapsul switch `Tertinggi` vs `Terendah`; (4) **Footer Dua Aksi**: Tombol `Reset` (mengembalikan filter ke default) dan tombol ungu `Terapkan Filter` (mengaplikasikan perubahan, mengunci state, dan menutup sheet). Mengeliminasi kepadatan toolbar di mobile sehingga tampilan tetap elegan dan bebas clutter. |
| **Laporan Keuntungan / Profit** | `profit.html`, `.profit-table-footer` | Halaman analisis profit dan margin keuntungan toko: header dengan ikon koin hijau, tombol Print & Filter. Empat kartu hero fokus laba (**Total Profit** hijau Rp 192.800, **Rata-rata Profit** indigo Rp 27.543, **Margin Kotor** orange 28.04%, **Transaksi Terbaik** cyan TRX-O7CVEG5R) dengan watermark SVG. Tabel data 8 kolom (NO, INVOICE, TANGGAL, KASIR, PELANGGAN, ITEM, PENJUALAN, PROFIT) dilengkapi baris **Total Profit** di sudut kanan bawah tabel. Terkoneksi mandiri melalui kategori LAPORAN di sidebar |
| **Fixed-Width Anti-Jitter Datepicker Capsule (Desktop)** | `laporan.html`, `styles.css`, `.rpt-period-group`, `.rpt-datepicker-btn`, `.rpt-dp-label` | **Stabilitas Lebar Kontrol Periode Desktop (Zero-Layout Shift)**: Menetapkan lebar tetap 268px (`box-sizing: border-box; justify-content: space-between`) pada tombol pemicu kalender `.rpt-datepicker-btn` dan centering pada `.rpt-dp-label`. Mengeliminasi fluktuasi/lonjakan lebar pada `.rpt-period-group` saat pengguna beralih antara Harian ("Sen, 07 Sep 2026 (Hari Ini)"), Mingguan ("07 Sep - 13 Sep 2026 (Minggu Ini)"), dan Bulanan ("September 2026 (Bulan Ini)"), menghasilkan lebar kapsul yang solid dan stabil persis 569px di resolusi desktop. |
| **Export PDF Laporan Tab-Aware & Decoupled Dataset (Enterprise Print)** | `laporan.html`, `styles.css`, `.rpt-btn-export-pdf`, `.rpt-btn-tab-export`, `exportReportPDF()` | **Ekspor Dokumen PDF Resmi Lengkap & Bersih (Decoupled dari Filter Bawah)**: Tombol aksi ekspor PDF yang ditempatkan di samping kontrol periode header (`.rpt-btn-export-pdf`) dan toolbar tab (`.rpt-btn-tab-export`). Saat diklik, fungsi `exportReportPDF()` mengekspor **seluruh dataset lengkap** sesuai tab aktif (Rekap Penjualan, Log Item, atau Log Transaksi) murni berdasarkan Toko dan Periode terpilih, **tanpa terpengaruh filter pencarian, filter kategori, maupun metode bayar di bawah tabel**. Hal ini menjamin angka omzet, profit, dan total barang di PDF 100% akurat dan identik dengan Bento KPI Grid eksekutif di header. Tampilan dokumen PDF dibuat bersih, resmi, dan profesional berstandar akuntansi A4 dengan kop resmi Amar Plastik, strip metadata 3 kolom elegan (**Periode**, **Lokasi Cabang**, dan **Waktu Cetak** — tanpa menampilkan filter aktif), kartu ringkasan KPI, tabel data lengkap bergaris zebra dengan grand total, serta blok tanda tangan pengesahan (Petugas Kasir & Store Manager). |
| **Master Kategori 2026 (Zero-Emoji & Glassmorphism Stamp)** | `kategori.html`, `styles.css`, `.cat-kpi-grid`, `.cat-grid-2026`, `.cat-card-2026`, `.cat-glass-stamp`, `.cat-mini-toggle` | **Taksonomi Departemen Modern 2026 Tanpa Emoji (100% Clean Vector SVG)**: (1) **Bento KPI Header**: Metrik Total Kategori, Aktif di Kasir, dan Total SKU Terdaftar; (2) **Glassmorphism Stamp Cards**: Ikon vektor terkurasi (Wheat/Bahan Kue, Layers/Plastik, Package/Kemasan) dengan latar belakang warna pastel gradien halus (`amber`, `blue`, `emerald`, `purple`, `rose`, `indigo`); (3) **Badge Monospace Kode Kategori**: Kode unik sistematis (`CAT-KUE`, `CAT-PLS`, `CAT-KMS`); (4) **Switch Toggle Visibilitas POS**: Sakelar slider ramping untuk menyembunyikan/menampilkan kategori di mesin kasir tanpa menghapus data; (5) **Shortcut Integrasi**: Tombol direct-link *Lihat Produk (N) ↗* yang membuka `produk.html?cat=id` terfilter; (6) **Modal CRUD Persisten**: Dialog form tambah/edit dengan icon picker dan *Live Preview* tab kasir real-time. |
| **Master Data Produk Multi-Toko & Capsule Store Dropdown** | `produk.html`, `styles.css`, `.rpt-store-dropdown-wrap`, `.prod-stock-cell`, `.prod-branch-pill`, `#btnStoreDropdown` | **Manajemen Inventaris Multi-Outlet Skalabel (Puluhan Cabang)**: (1) **Capsule Store Dropdown**: Mengadopsi filter toko dari laporan dengan fitur *instant search* cabang toko untuk skalabilitas puluhan cabang; (2) **Dua Mode Pemantauan Stok**: Mode *"Semua Toko"* menyajikan Total Konsolidasi (`48 pcs Total`) dengan chip interaktif `[ 3 Cabang ▾ ]`, sedangkan mode *Cabang Tertentu* (mis. Cabang 2) menyajikan stok khusus cabang tersebut beserta status stoknya (*Aman*, *Menipis*, *Habis*); (3) **Zero-Emoji Policy**: Visual produk menggunakan ikon vektor kategori pada bubble pastel; (4) **Modal CRUD Produk**: Pendaftaran SKU baru dengan form alokasi stok awal per cabang toko yang dinamis. |
| **Popover Rincian Distribusi Stok Cabang** | `produk.html`, `styles.css`, `.breakdown-modal`, `.breakdown-header-card`, `.breakdown-list`, `.breakdown-row` | **Audit Stok Lintas Cabang Sekali Klik**: Ketika pengguna mengklik pill `[ 3 Cabang ▾ ]` pada baris atau kartu produk, popover dialog modern terbuka menampilkan: (1) Header nama produk, kode SKU, kategori, dan angka total konsolidasi; (2) Bilah pencarian instan nama cabang; (3) Daftar outlet terperinci beserta alamat dan jumlah stok riil cabang; (4) Shortcut tombol *"Sesuaikan Stok Produk Ini"* yang langsung memicu modal adjustment. |
| **Modal Penyesuaian Stok 3-Mode & Mutasi Persisten** | `produk.html`, `shared.js`, `styles.css`, `.adjust-modal`, `.adjust-mode-tabs`, `recordStockMutation()` | **Modul Operasional Update Stok Cabang Terpadu**: Tombol aksi dedicated `[ 📦 Stok ]` pada tabel dan kartu produk yang membuka modal penyesuaian stok dengan: (1) **Selector Cabang Toko**: Dropdown pemilihan lokasi cabang (otomatis terisi sesuai filter aktif); (2) **3 Mode Tab**: *Restock (+N)* untuk barang masuk supplier, *Rusak/Expired (-N)* untuk barang susut/afkir, dan *Opname Fisik (=N)* untuk sinkronisasi hasil hitung fisik di toko; (3) **Live Formula Preview**: Menampilkan kalkulasi real-time `Stok Awal + Masuk = Stok Akhir`; (4) **Log Mutasi Persisten (`ap_stock_mutations`)**: Mencatat id mutasi unik, tanggal-jam, nama cabang, delta, qty awal/akhir, dan nomor referensi surat jalan. |
| **Product Image Uploader & Responsive Modal Layout (Desktop, Tablet, Mobile)** | `produk.html`, `styles.css`, `.prod-modal-body`, `.prod-modal-top-section`, `.prod-img-uploader`, `.prod-img-dropzone`, `.prod-img-preview-box`, `.prod-img-remove-btn`, `#prodImageUrlInput`, `#prodImageFileInput` | **Komponen Input Gambar Produk Fleksibel & Tata Letak Modal Responsif**: (1) **Dua Metode Input**: Mendukung Drag-and-Drop file picker dari komputer/HP (JPG, PNG, WEBP hingga 3MB dengan konversi Base64 otomatis via `FileReader`) dan input langsung URL/tautan gambar eksternal; (2) **Live Preview Box**: Kotak pratinjau instan dengan tombol floating bulat hapus foto (`.prod-img-remove-btn`) berikon silang SVG bersih; (3) **Tata Letak Adaptif 3 Breakpoint**: Desktop (>840px) 2-kolom seimbang (area foto 210px di kiri, metadata nama/SKU/kategori di kanan), Tablet (641px–840px) 180px di kiri, dan Mobile (≤640px) ditransformasikan menjadi 1-kolom stacked vertikal dengan dropzone rasio 16:9 yang ramah sentuhan (touch-friendly); (4) **Zero-Emoji Policy**: Menggunakan ikon kamera/gambar dan silang 100% vektor SVG. |
| **Thumbnail & Kartu Produk Terintegrasi Gambar** | `produk.html`, `styles.css`, `.p-thumb.has-img`, `.p-thumb-img`, `.prod-card-top.has-img`, `.prod-card-full-img` | **Rendering Gambar Produk di Tampilan Tabel & Kartu (Table & Card View)**: (1) **Tabel**: Thumbnail gambar rounded square 38x38px (`object-fit: cover`) ber-border halus di samping nama dan SKU produk; (2) **Kartu (Card Grid)**: Foto produk mengisi penuh area kartu atas (`.prod-card-full-img`, 16:10) dilengkapi subtle gradient overlay (`rgba(15, 23, 42, 0.35)`) di bagian atas dan bawah agar badge stok dan tombol aksi (Edit, Hapus, Sesuaikan Stok) berlatar semi-transparan tetap kontras tinggi dan mudah diklik; (3) **Smart Fallback**: Produk tanpa gambar tetap otomatis menampilkan ikon kategori SVG di atas latar pastel gradien secara konsisten. |
| **Kalkulasi HPP Rata-Rata Bergerak (Weighted Average Costing / WAC)** | `produk.html`, `styles.css`, `#adjustCostGroup`, `#adjustCostInput`, `#adjustHppCalcRow`, `#adjustHppFormula`, `#adjustHppBadge` | **Otomasi Biaya Perolehan Persediaan Berbasis Standar Akuntansi WAC (Moving Average)**: (1) **Input Harga Beli Baru**: Field input `#adjustCostInput` muncul otomatis saat mode *Restock (+)* aktif, terisi default dengan HPP saat ini; (2) **Live Interactive WAC Preview**: Box kalkulasi menampilkan simulasi langsung rumus WAC $((S_{\text{lama}} \times \text{HPP}_{\text{lama}}) + (\Delta S \times \text{HPP}_{\text{masuk}})) / (S_{\text{lama}} + \Delta S)$, menampilkan angka transisi `Rp A → Rp B` dan badge selisih `+Rp N` (biru) atau `-Rp N` (kuning); (3) **Pembaruan Otomatis**: Saat disimpan, `p.cost` terupdate ke nilai rata-rata tertimbang baru dan tercatat lengkap di log mutasi persisten. |
| **Batch Restock Multi-Item Modal (Penerimaan Barang Massal Satu Toko)** | `produk.html`, `styles.css`, `#btnBatchStock`, `#batchStockModalBackdrop`, `.batch-modal`, `.batch-doc-grid`, `.batch-table`, `.batch-summary-bar` | **Efisiensi Penerimaan Barang Grosir/Supplier Multi-Item (Enterprise 2026)**: (1) **Tombol Pemicu Dedicated**: Tombol `#btnBatchStock` berikon truk/box SVG di samping tombol Tambah Produk; (2) **Metadata Dokumen**: Input cabang toko penerima, nomor dokumen/PO/Surat Jalan, tanggal penerimaan, dan nama supplier; (3) **Tabel Restock Interaktif**: Kontrol input kuantitas masuk & harga beli baru per baris dengan live auto-calculation; (4) **Bar Ringkasan**: Menghitung otomatis Total Produk, Total Unit Masuk, dan Estimasi Nilai Pembelian; (5) **Batch Execution**: Menerapkan pertambahan stok dan pembaruan WAC untuk seluruh item sekaligus dalam satu transaksi, merekam log mutasi per item dengan nomor referensi dokumen yang sama. |
| **Searchable Combobox, 1-Click Auto-Add & Auto-Close (Batch Restock)** | `produk.html`, `styles.css`, `.batch-combobox-wrap`, `.batch-combobox-trigger`, `.batch-combobox-dropdown`, `.batch-combobox-search-input`, `.batch-combobox-list`, `.batch-combobox-item` | **Pencarian Produk Cepat & Otomasi Masuk List (Zero Extra Clicks)**: (1) **Modern Searchable Combobox**: Menggantikan elemen `<select>` native dengan custom popover combobox yang elegan. Dilengkapi badge counter jumlah produk tersedia (`N Tersedia`), ikon search loupe SVG, dan dropdown melayang beranimasi; (2) **Live Instant Search**: Input pencarian real-time internal (`Ketik nama, SKU, atau kategori...`) dengan tombol reset clear `✕`; (3) **1-Click Auto-Add & Auto-Close**: Mengklik sembarang produk pada dropdown otomatis mendaftarkannya ke dalam tabel batch restock dengan kuantitas default 10 unit dan HPP default, dan **dropdown popover seketika tertutup otomatis** sehingga pengguna langsung berfokus memeriksa baris tabel yang baru ditambahkan; (4) **Eliminasi "Masukkan Semua"**: Menghapus tombol *Masukkan Semua* agar daftar restock tetap terkontrol dan berfokus pada barang yang riil diterima. |
| **Kosongkan Seluruh List Batch Persisten & Disabled State** | `produk.html`, `styles.css`, `.btn-batch-clear-all`, `#btnBatchClearAll`, `#batchClearConfirmModalBackdrop`, `.batch-confirm-modal` | **Pembersihan Cepat Daftar Restock dengan Proteksi Data**: (1) **Tombol Selalu Tampak (Persistent Disabled State)**: Tombol `.btn-batch-clear-all` ("Kosongkan List") tetap selalu terlihat di toolbar. Saat daftar batch kosong, tombol berada dalam kondisi `disabled` (berwarna abu-abu, `opacity: 0.4`, `cursor: not-allowed`); saat daftar memiliki minimal 1 item, tombol aktif dengan outline merah elegan; (2) **Modal Dialog Konfirmasi**: Menampilkan alertdialog konfirmasi (`#batchClearConfirmModalBackdrop`) dengan rincian jumlah item yang akan dihapus untuk mencegah kehilangan data akibat klik tak sengaja; (3) **Aksi Konfirmasi & Batal**: Menghapus seluruh array batch saat dikonfirmasi, mengembalikan tombol ke kondisi `disabled`, dan menampilkan feedback toast informatif. |
| **Dual-Mode Input Harga Beli (Toggle Satuan vs Subtotal)** | `produk.html`, `styles.css`, `#batchGlobalCostMode`, `.batch-cost-mode-btn`, `.batch-cost-cell`, `.batch-row-mode-switch`, `.batch-mini-pill`, `.batch-cost-sub-hint` | **Fleksibilitas Input Biaya Pembelian Faktur/Supplier (Satuan vs Subtotal)**: (1) **Header Master Toggle**: Segmented switch `[ Satuan | Subtotal ]` di header kolom Harga Beli untuk mengubah mode input seluruh baris sekaligus; (2) **Row-Level Mini Switch**: Setiap baris item memiliki mini pill segmented `[ Satuan | Subtotal ]` independen; (3) **Mode Satuan (Unit)**: Pengguna menginput harga beli per pcs (`Rp 10.580`), subtotal dihitung otomatis; (4) **Mode Subtotal**: Pengguna menginput total nota belanja baris tersebut (mis. `Rp 150.000` untuk 10 pcs), sistem otomatis membagi dengan kuantitas untuk mendapatkan harga per unit (`Rp 15.000`) dan memperbarui kalkulasi WAC seketika; (5) **Live Hint**: Di bawah input selalu tertera informasi pelengkap dinamis (`Satuan: Rp N • HPP lama: Rp M`). |
| **Proyeksi Kuantitas Stok & HPP Live per Item (Indikator Warna Merah/Hijau)** | `produk.html`, `styles.css`, `.batch-impact-cell`, `.batch-proj-stock`, `.batch-proj-hpp`, `.hpp-after-up`, `.hpp-after-down`, `.badge-up`, `.badge-down`, `.batch-proj-delta-tag` | **Visibilitas Dampak Kuantitas & Biaya Riil per Baris dengan Color-Coding Status**: Setiap baris tabel restock menampilkan 3 parameter proyeksi simultan: (1) **Proyeksi Jumlah Stok Cabang**: Visualisasi transisi kuantitas saat ini $\rightarrow$ kuantitas akhir setelah restock (`Stok: 15 → 25 pcs (+10)`) dengan badge delta hijau terang (`+N`); (2) **Proyeksi HPP (WAC) dengan Indikator Warna**: Jika HPP rata-rata tertimbang baru **NAIK**, teks angka dan badge delta otomatis berwarna **MERAH** (`#dc2626`, misal `↑ +Rp 567`); jika HPP **TURUN**, teks angka dan badge delta otomatis berwarna **HIJAU** (`#059669`, misal `↓ -Rp 720`); jika tetap, berwarna netral (`Tetap`); (3) **Subtotal Pembelian**: Perkalian instan kuantitas masuk dengan harga beli baru; seluruhnya terupdate secara reaktif saat kuantitas atau harga diedit pengguna. |
| **Penyuntingan Langsung Harga Jual & Live Margin Preview (Batch Restock)** | `produk.html`, `styles.css`, `.batch-input-selling-price`, `.batch-selling-margin-hint` | **Fleksibilitas Penetapan Harga Jual Berdasarkan Fluktuasi HPP**: (1) **Kolom Harga Jual Interaktif**: Kolom terpisah pada tabel batch restock dengan input box editable yang terisi default sesuai harga jual aktif produk; (2) **Live Margin Preview Hint**: Tepat di bawah input harga jual, sistem secara otomatis menghitung dan menampilkan estimasi margin keuntungan (`Margin: +Rp N (P%)` berwarna hijau) atau peringatan potensi kerugian (`Rugi: -Rp N` berwarna merah tebal) terhadap proyeksi HPP baru; (3) **Persistensi Pembaruan Harga**: Saat batch restock diterapkan, perubahan harga jual otomatis disimpan ke master data produk (`masterProducts`) dan tersimpan permanen di `localStorage`. |
| **Sistem Multi-Satuan / Kemasan & Modal Option B (Kasir POS)** | `kasir.html`, `app.js`, `styles.css`, `.unit-picker-backdrop`, `.unit-picker-modal`, `.unit-option-card`, `.unit-multi-pill`, `.badge.is-multi`, `.card-unit-indicator` | **Arsitektur Multi-Kemasan Toko Plastik & Bahan Kue (Option B Popup Modal)**: (1) **Single Base-Unit Source of Truth**: Seluruh stok produk dikelola dan disimpan dalam satuan terkecil (`gr` untuk bahan timbang, `pcs` untuk plastik/kemasan hitung); (2) **Etalase Interaktif Kasir**: Produk dengan multi-satuan (seperti Gula Pasir 50kg yang dijual eceran 500gr, 1kg, 2kg, dan karung 50kg) menampilkan badge ungu `N Satuan` dan tombol hijau `[ Pilih Satuan (N) ]`. Mengklik kartu langsung memunculkan **Modal Option B (Popup Sheet Terpadu)**; (3) **Modal Option B**: Menampilkan foto produk, kategori, sisa stok cabang dalam format dual-unit (`75 kg (1 Karung + 25 kg)`), serta kartu pilihan tiap varian satuan berisi nama varian, rasio konversi (`1 kg = 1.000 gr`), harga normal tercoret, tag diskon, harga final rounded, dan proteksi disable otomatis jika sisa stok tidak mencukupi; (4) **Kompatibilitas Keranjang Multi-Varian**: Keranjang menggunakan composite key `${productId}__${unitId}` sehingga pelanggan dapat membeli varian kemasan 1 kg dan varian eceran 500 gr secara bersamaan dalam 1 nota; (5) **Pengurangan Stok Otomatis & Cetak Struk**: Transaksi mengurangi stok cabang Pusat berdasarkan `qty * qtyRatio` dan mencetak varian kemasan lengkap pada struk belanja (`Gula Pasir 1kg (Bungkus 1 kg)`). |
| **Aturan Pembulatan Harga Diskon 500 Rupiah ke Bawah** | `shared.js`, `roundDown500()`, `calcFinalPrice()` | **Formula Pembulatan Kasir Retail Tradisional (Anti Uang Koin Kecil)**: Menghilangkan pecahan koin ganjil pada harga setelah diskon dengan pembulatan ke bawah ke kelipatan 500 terdekat: `Math.floor(discountedPrice / 500) * 500`. Sebagai contoh, Gula 1kg Rp 16.500 dengan diskon 5% menghasilkan Rp 15.675 yang dibulatkan turun menjadi tepat **Rp 15.500**. Menjamin kemudahan transaksi kasir tunai tanpa kerepotan uang kembalian koin nominal kecil. |
| **Format Tampilan Stok Bersih Standar Toko (Eliminasi Batch dalam Kurung)** | `shared.js`, `produk.html`, `formatDualUnitStock()` | **Penyederhanaan Tampilan Kolom Stok Toko Bebas Tanda Kurung**: Mengeliminasi penjabaran kemasan/batch dalam tanda kurung (seperti `(2 Sak 25 kg + 4,999 kg)` atau `(22 Kemasan 1 kg)`) agar kolom STOK TOKO ringkas, bersih, dan mudah dibaca kasir/staf. Sistem menampilkan angka murni dalam satuan standar acuan: **`kg`** (untuk berat), **`pcs`** (untuk barang cacahan), **`lembar`** (untuk kertas/mika/plastik lembaran), dan **`liter`** (untuk volume). Contoh: `55 kg`, `139 kg`, `23 pcs`, `22 kg`, `450 pcs`. |
| **Repeater Variasi Jual & Kemasan Eceran Dinamis (Tambah/Edit Produk)** | `produk.html`, `styles.css`, `#prodVariationsSection`, `#btnAddProdVariation`, `#prodVariationsList`, `.prod-var-row`, `.prod-var-measure-combo`, `.prod-var-disc-combo`, `.prod-var-final-box` | **Konfigurasi Variasi Jual Fleksibel dengan Live Auto-Calculation & Diskon 500 ke Bawah**: (1) **Komponen Repeater Multi-Baris**: Pengguna dapat menambahkan opsi variasi jual tak terbatas (misal 250 gr, 500 gr, 1 kg, 2 kg, karung 50 kg) via tombol `+ Tambah Variasi`; (2) **Input Takaran & Dropdown Satuan Bersebelahan**: Input numerik (mis. `250`) dipasangkan langsung dengan dropdown satuan terkait (misal `gr` atau `kg` jika produk dasar kg; `ml` atau `liter` jika cairan; `pcs`, `pack`, `dus`, `lusin` jika kemasan); (3) **Kalkulasi Harga Jual Proporsional Otomatis**: Sistem secara reaktif menghitung harga normal berdasarkan rasio terhadap harga jual dasar produk (misal jika 1 kg = Rp 16.000, maka 250 gr otomatis terisi Rp 4.000), namun pengguna tetap leluasa mengedit harga tersebut secara manual (auto-price override); (4) **Kolom Diskon Terpadu**: Mendukung diskon persen (`%`) atau nominal (`Rp`) per varian dengan live preview harga final yang otomatis menerapkan aturan pembulatan 500 Rupiah ke bawah (`roundDown500`); (5) **Sinkronisasi Reaktif**: Perubahan pada satuan produk utama atau harga jual dasar akan memperbarui variasi secara cerdas dan tersimpan permanen dalam properti `units` master produk. |
| **Normalisasi HPP Satuan Standar Pasar Tanpa Koma/Desimal** | `shared.js`, `produk.html`, `formatDisplayHpp()`, `formatDualUnitStock()` | **Standarisasi Tampilan HPP Bahan Baku Sesuai Acuan Pasaran**: Mengubah angka pecahan HPP mikro (misal `Rp 12 / gram` pada Gula atau `Rp 9.5 / gram` pada Terigu) menjadi satuan acuan standar pasar: **1 kg** (untuk berat/gram), **1 liter** (untuk volume/ml), **1 pcs** (untuk plastik/cup), dan **1 lembar** (untuk box/mika). Seluruh angka dibulatkan utuh bulat tanpa desimal atau koma (`Math.round(cost * multiplier)`) menjadi `Rp 12.000 / kg`, `Rp 9.500 / kg`, `Rp 110 / pcs`, dan `Rp 150 / lembar`, baik pada tampilan Master Produk (tabel & kartu) maupun proyeksi WAC di modal Batch Restock. |

## Interaksi

- Semua transisi 0.15–0.28s ease; dihormati `prefers-reduced-motion`
- **Interaksi Buka/Tutup Sidebar**:
  - **Auto-Close saat Klik Button**: Baik pada versi Desktop maupun Mobile, ketika sidebar sedang terbuka dan pengguna mengklik tombol/item navigasi di sidebar (`.rail-btn`, `.rail-brand`), sidebar akan otomatis menutup kembali (`toggleSidebar(false)`).
  - **Auto-Open saat Klik di Luar Button**: Pada versi Desktop dan Tablet (`>768px`), ketika sidebar sedang dalam kondisi tertutup (mode strip ramping 72px), mengklik area sidebar rail di luar button/link akan otomatis membuka/memperlebar sidebar (`toggleSidebar(true)`). Kursor rail otomatis menampilkan `cursor: pointer`.
  - **Auto-Close saat Klik Luar**: Mengklik area konten di luar sidebar saat sidebar terbuka akan menutup sidebar secara otomatis.
  - **Keyboard Shortcut**: `Ctrl+B` / `Cmd+B` untuk toggle dan `Escape` untuk menutup sidebar.
- Hover kartu: naik 4px + shadow md
- Focus visible: outline 2px `--accent` offset 2px
- Pajak 10% dihitung dari total harga jual
- Diskon Produk: selisih harga normal (`old`) vs harga jual per item; Subtotal menampilkan harga normal, diskon tampil sebagai `− Rp N` berwarna aksen. Item berdiskon juga menampilkan catatan hijau "Hemat Rp N" (`.oi-save`) di panel pesanan
- **Batas stok**: qty di keranjang maksimal sama dengan stok produk. Guard ada di `addToCart()` (klik kartu) dan `changeQty()` (tombol +) — melebihi stok ditolak dengan toast "Stok … hanya N". Saat qty = stok, tombol + pada item keranjang `disabled` (opacity .35, cursor not-allowed, title "Stok maksimal")
- **Batal pesanan**: tombol "🗑 Hapus Semua" memunculkan **popup modal** (ikon 🗑, merah) berisi jumlah item; dikonfirmasi → keranjang kosong + toast. Tombol nonaktif saat keranjang kosong
- **Konfirmasi transaksi**: klik "Proses Transaksi" memunculkan **popup modal** (ikon 🛒, hijau) berisi ringkasan Jumlah item / Total / Pembayaran; bila **Cash**, modal menampilkan box input uang — **default terisi sesuai total belanja** (teks ter-select agar kasir bisa langsung mengetik koreksi), tombol cepat, dropdown nominal, atau input manual; kembalian dihitung langsung di mini tabel Total/Dibayar/Kembalian (dibayar − total); uang kurang ditolak. Batal = keranjang tetap utuh
- **Struk**: setelah konfirmasi ya, popup struk hitam-putih muncul otomatis (fungsi `showReceipt()`) dengan kode transaksi unik `TRX-YYYYMMDD-XXXX` dan waktu saat ini. Baris **Dibayar (metode)** dan **Kembalian** selalu tampil: tunai = nominal riil yang diinput, Debit/QRIS = sama dengan total sehingga kembalian Rp 0. Footer tombol tidak ikut terscroll (`flex` + `receipt-body` scroll mandiri). Tombol **Cetak** stub printer (toast), tombol **Selesai** menutup struk

## Panel Order (Detail Item)
- Panel tinggi 100vh dengan `overflow: hidden`; hanya `.order-list` yang scroll (`flex: 1; overflow-y: auto`) — bagian total, metode bayar, dan CTA selalu terlihat walau banyak item
- Item memakai grid `44px | 1fr | auto` dua baris: nama produk terpotong ellipsis (`text-overflow: ellipsis`), harga rata kanan dan tidak bergeser oleh nama panjang
- Divider tipis antar item; scrollbar tipis (`scrollbar-width: thin`)

## Konten
- Toko: **Amar Plastik** — bahan pembuatan kue (tepung, gula, mentega, coklat bubuk) & perlengkapan plastik/kemasan (kresek, mica bento, cup puding, plastik vacuum)
- Semua label UI berbahasa Indonesia: "Pilih Produk", "Detail Item", "Metode Pembayaran", "Proses Transaksi"
